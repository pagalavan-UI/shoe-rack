# Local development environment variables
# Copy values from .env.example and customize for your machine.
PORT=5173
BASE_PATH=/ 
# Backend base URL (used in browser via `import.meta.env.VITE_API_URL`)
# Example: http://localhost:4000
VITE_API_URL=http://localhost:4000
