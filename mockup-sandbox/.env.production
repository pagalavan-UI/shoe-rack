# Production / server environment for mockup-sandbox
# Adjust this for your deployed server environment.
PORT=5173
BASE_PATH=/ 
# Backend base URL (used in browser via `import.meta.env.VITE_API_URL`)
VITE_API_URL=https://api.example.com
