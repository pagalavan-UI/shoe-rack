# Local development environment for mockup-sandbox
# Customize these values for your local machine.
PORT=5173
BASE_PATH=/ 
# Backend base URL (used in browser via `import.meta.env.VITE_API_URL`)
VITE_API_URL=http://localhost:4000
