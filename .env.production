# Production / server environment variables
# Set these before deploying your production build.
PORT=5173
BASE_PATH=/ 
# Backend base URL (used in browser via `import.meta.env.VITE_API_URL`)
# Set this to your deployed API endpoint.
VITE_API_URL=https://api.example.com
